import { Page } from '@/components/site-shell';

export const metadata = { title: 'Pricing' };

export default function PricingPage() {
  return (
    <Page
      title="Pricing"
      lead="Plans are priced per institution and per product. Published tiers are being finalised — talk to us for a quote in the meantime."
    />
  );
}
