import { Page } from '@/components/site-shell';

export const metadata = { title: 'Contact' };

export default function ContactPage() {
  return (
    <Page
      title="Contact"
      lead="For sales, partnerships or support, reach us at hello@munaxa.com."
    />
  );
}
