import { Page } from '@/components/site-shell';

export const metadata = { title: 'Careers' };

export default function CareersPage() {
  return (
    <Page
      title="Careers"
      lead="We hire engineers who care about the parts of the system nobody sees. Open roles are posted here."
    />
  );
}
