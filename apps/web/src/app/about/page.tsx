import { Page } from '@/components/site-shell';

export const metadata = { title: 'About' };

export default function AboutPage() {
  return (
    <Page
      title="About Munaxa"
      lead="We build long-lived systems for institutions that need to be right rather than fashionable — starting in Jordan, in Arabic and English, right-to-left and left-to-right."
    />
  );
}
