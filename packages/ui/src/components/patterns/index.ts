/**
 * Composite patterns built from primitives (e.g. StatCard, DataTable, SearchBar).
 * Empty for now — domain/composite components are migrated here from the apps and the
 * design system in subsequent phases (see MIGRATION_REPORT.md).
 */
export {};
