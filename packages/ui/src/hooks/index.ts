/**
 * Shared React hooks for @munaxa/ui.
 *
 * Reusable hooks discovered across the apps and the design system (e.g. useMobile,
 * useComposition, usePersistFn) are migrated here so there is one implementation per hook.
 * Empty for now — populated as hooks are migrated (see MIGRATION_REPORT.md).
 */
export {};
