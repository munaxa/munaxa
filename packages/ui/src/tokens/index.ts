/**
 * Design tokens for @munaxa/ui — re-exported from the single source of truth,
 * @munaxa/design-tokens. Consumers may read tokens via `@munaxa/ui` or directly from
 * `@munaxa/design-tokens`; both resolve to the same values.
 */
export * from '@munaxa/design-tokens';
