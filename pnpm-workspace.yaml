# Munaxa Corporate — an independent workspace.
#
# The corporate website is a consumer of the shared platform exactly like every other
# product: @munaxa/* is resolved from the registry, not from a workspace member. This
# repository must never depend on School, Work or Docs.
packages:
  - 'apps/*'
