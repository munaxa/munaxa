# Marks this directory as its own pnpm root so installs never resolve against a
# parent workspace, and the lockfile lives here. Required for the standalone repo.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - workerd
