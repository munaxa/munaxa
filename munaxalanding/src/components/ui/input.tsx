/**
 * Re-export the canonical Input + Textarea from @munaxa/ui (single implementation).
 * Existing `@/components/ui/input` imports keep working unchanged.
 */
export { Input, Textarea } from '@munaxa/ui';
