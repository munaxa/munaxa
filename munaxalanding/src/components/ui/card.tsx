/**
 * Re-export the canonical Card family from @munaxa/ui (single implementation).
 * Existing `@/components/ui/card` imports keep working unchanged.
 */
export { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '@munaxa/ui';
